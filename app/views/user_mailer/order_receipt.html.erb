
<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Order ID: <%= @order.id %></h1>
    <p>You bought: </p>
    <% @order.line_items.each do |item| %>
      <p><%= item.quantity %> <%= item.product.name %> for <%= humanized_money_with_symbol item.product.price * item.quantity %> </p>
    <% end %>
    <h2>Total: <%= humanized_money_with_symbol @order.total_cents / 100 %>
    <p>Thank you for your business</p>
  </body>
</html>